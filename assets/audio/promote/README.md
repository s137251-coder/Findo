Drop a promotion fanfare here and it replaces the synthesised `promote.wav`.
Same rule as `ok`, `notok` and `music`: the folder is the interface, the pool
is read from the asset manifest at startup, and one is picked at random.
